import { actionTypesKeys } from './KEYS';

export const attachmentsActionsTypes = {
	SET_ITEMS: `${actionTypesKeys.ATTACHMENTS}.SET_ITEMS`,
	REMOVE_ITEM: `${actionTypesKeys.ATTACHMENTS}.REMOVE_ITEM`,
};
